🕒 Лабораторна робота №2 — Smart Timer (Qt, C++)

Цей проєкт є графічним застосунком, розробленим мовою C++ з використанням фреймворку Qt 6. Його мета — реалізувати систему «розумного таймера», яка дозволяє створювати, редагувати, запускати, зупиняти та зберігати кілька таймерів одночасно. Кожен таймер працює в реальному часі, автоматично оновлюється та виводить сповіщення після завершення відліку.

Основний функціонал програми включає: додавання нових таймерів із заданою назвою та тривалістю, запуск і зупинку кожного таймера окремо, автоматичне збереження всіх активних таймерів у файл data/timers.json, відновлення їх під час наступного запуску програми, а також оновлення інтерфейсу щосекунди для відображення залишку часу.

Програма створена з використанням CMake і може запускатися у середовищі CLion або Qt Creator. Для побудови інтерфейсу та роботи з даними використано класи Qt: QMainWindow, QTimer, QDateTime, QJsonDocument тощо.

Основні класи:

Timer — описує окремий таймер, зберігає його назву, тривалість, стан та має методи start(), stop(), tick().

TimerManager — керує колекцією таймерів, зберігає їх у файл, завантажує та оновлює час у реальному часі.

MainWindow — головне вікно програми, яке відображає список таймерів і забезпечує взаємодію з користувачем.

AddEditDialog — діалогове вікно для додавання та редагування таймерів.

Для запуску програми потрібно відкрити проєкт у CLion або Qt Creator, зібрати його за допомогою CMake та натиснути кнопку Run ▶️. У головному вікні можна додати новий таймер, керувати його запуском чи зупинкою, а також зберегти всі дані.

Автор: Білоножко Артем Олександрович 
Група: ІПС-22
2025 рік, Київ.
